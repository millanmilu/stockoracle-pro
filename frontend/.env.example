# Copy this file to frontend/.env.local and set real values.
# Never commit .env.local.

# Backend URL (defaults to production if unset)
VITE_API_URL=http://localhost:8000

# Optional: static API key matching API_KEY in backend/.env
# Leave empty to disable auth (open API).
VITE_API_KEY=
